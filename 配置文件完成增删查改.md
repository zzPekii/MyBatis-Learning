# 4. 配置文件完成增删查改

## 案例：

## 需要实现的操作

1. 查询
    1. 查询所有数据
    2. 查看详情
    3. 条件查询
2. 添加
3. 修改
    1. 修改全部
    2. 修改动态字段
4. 删除
    1. 删除一个
    2. 批量删除

[数据库注入中文乱码问题](https://www.notion.so/326eee29951a80faa8a7fc3476b2bc82?pvs=21)

## 4.1.a 数据库变量与类变量的映射问题 —— 结果映射

*当我们在`CLASSMapper.xml`中添加语句的时候，我们会与到数据库的变量名和数据类的变量名不一致，我们可以用2中方法去改进*

### 4.1.a.(1) 起别名（SQL片段）

直接在SQL语句中用 `as` 去改名

```xml
<select id="selectAll" resultType="Brand">
    SELECT id, brand_name AS brandName, company_name AS companyName FROM tb_brand;
</select>
```

当然也可以把这一长串声明为一个SQL片段

```xml
<sql id="brand_column">
    id, brand_name as brandName, company_name as companyName, ordered, description, status
</sql>

<select id="selectAll" resultType="Brand">
    SELECT <include refid="brand_column" /> FROM tb_brand;
</select>
```

### 4.1.a.(2) ResultMap （最常用的方案）

在 xml 中定义映射规则，讲数据库的 `column` 指向实体类的 `property` 

- **特点**：`select` 标签中不再使用 `resultType`，而是改用 `resultMap`。
- **规则**：主键建议用 `<id>`，普通字段用 `<result>`。**只需定义不一致的字段**，名称一致的字段 MyBatis 会自动映射。

```xml
<resultMap id="brandResultMap" type="com.harris.pojo.Brand">
    <result column="brand_name" property="brandName" />
    <result column="company_name" property="companyName" />
</resultMap>

<select id="selectAll" resultMap="brandResultMap">
    SELECT * FROM tb_brand;
</select>
```

> **Gemini：终极懒狗法**：如果你的项目遵循标准命名规范（数据库下线 `brand_name` → Java 驼峰 `brandName`），可以在 MyBatis 全局配置文件中开启 `mapUnderscoreToCamelCase` 自动转换，这样连 ResultMap 都可以不写了。
> 

## 4.1.b 查询详情（description）

```xml
<!-- selectById --> 
<!--
    参数占位符
    1. #{}: 会将其替换为?, 为了防止sql注入
    2. ${}: 拼sql，会存在sql注入的问题
-->
<select id="selectById" resultMap="brandResultMap">
    SELECT * FROM tb_brand WHERE id = #{id};
</select>
```

### 4.1.b.(1) 传参的问题

一般都用 `#` 

`${x}` 在表明或者列明不固定的情况下使用，会存在sql注入的问题

### 4.1.b.(2) 参数类型

```xml
<select id="selectById" parameterType="int" resultMap="brandResultMap">
    SELECT * FROM tb_brand WHERE id = #{id};
</select>

<!-- 其实时可以省略不写的 -->
```

### 4.1.b.(3) 特殊字符的处理 —— 转译字符&CDATA区

```xml
<select id="selectById" resultMap="brandResultMap">
    SELECT * FROM tb_brand WHERE id > #{id};
</select>
```

但是我们可以用转译字符去处理 <

```xml
<select id="selectById" resultMap="brandResultMap">
    SELECT * FROM tb_brand WHERE id &lt; #{id};
</select>
```

CDATA区，把字符放到CDATA区，xml就会把他当成纯文本来处理，适用于有很多的特殊字符

```xml
<select id="selectById" resultMap="brandResultMap">
    SELECT * FROM tb_brand WHERE id 
	    <![CDATA[
		    <
		    ]]>
	     #{id};
</select>
```

## 4.1.c 多条件查询

```java
// 我们在interface里面定义方法
// 3种定义的方法
public List<Brand> selectByCondition(@Param("status")int status, @Param("companyName")String companyName, @Param("BrandName")String BrandName);
public List<Brand> selectByCondition(Brand brand);
public List<Brand> selectByCondition(Map map);
```

```xml
<!-- selectByCondition --> 

<select id="selectByCondition" resultMap="brandResultMap">
    SELECT *
    FROM tb_brand
    WHERE status = #{status}
        AND company_name like #{companyName}
        AND brand_name like #{brandName}
</select>    
```

### 4.1.c.(1) Param注解

### 4.1.c.(2) 对象参数：对象的属性名称要和参数占位符一致

在调用的时候，把传入参数封装成对象

```java
// interface下
public List<Brand> selectByCondition(Brand brand);

// 测试类下
// 处理参数
companyName = "%" + companyName + "%";
brandName = "%" + brandName + "%";
// 封装成对象
Brand brand = new Brand();
brand.setBrandName(brandName);
brand.setId(id);
brand.setCompanyName(companyName);

// ... 准备sql session
List<Brand> brands = brandMapper.selectByCondition(brand);
System.out.println(brands);
```

主要要根据处理的参数去实现

### 4.1.c.(3) Map类

```java
// interface下
public List<Brand> selectByCondition(Map map);

// 测试类下
// 2. 封装参数到 Map
Map<String, Object> map = new HashMap<>();
map.put("status", status);
map.put("companyName", companyName);
map.put("brandName", brandName);

List<Brand> brands = brandMapper.selectByCondition(map);
System.out.println(brands);
```

### 4.1.c Summary

1. 散装参数：需要使用@Param()sql参数占位符
2. 实体封装参数：只需要保证sql中的参数名 和 类试题属性名对应上
3. 只需要保证SQL中的参名和map集合的键的名称对应上

## 4.2 添加

添加的时候我们要传入除了id意外的所有数据

返回的结果为 `void` 

xml下面对应的sql语句

```xml
<insert id="add">
		INSERT INTO tb_brand(brand_name, company_name, ordered, description, status)
		VALUES (#{brandName}, #{companyName}, #{ordered}, #{description}, #{status})
</insert>
```

但是在提交的时候，默认没有自动提交事务，这样在表内就看不到我们新插入的行，需要在运行完之后，再运行`sqlSession.commit();` ，或者在创建`SQLSession`对象的时候就把`autocommit`设定为`True`

```java
SqlSession sqlSession = sqlSessionFactory.openSession(true);
```

### 4.2.1 主要键返回

在数据添加成功后，需要获取数据库数据的`Primary Key`的值

返回添加数据的主键，其实也就是两个属性

```java
<insert useGeneratedKeys="true" keyProperty="id">
```

## 4.3 修改 —— update

### 4.3.a 修改全部字段

返回类型可以是`int`也可以是`void`

返回为`int`时，代表的是修改所影响的字段

```java
public int update(Brand brand);
```

```xml
<update id="update">
    UPDATE tb_brand
        SET brand_name = #{brandName},
            company_name = #{companyName},
            ordered = #{ordered},
            description = #{description},
            status = #{status}
        WHERE id = #{id}
</update>
```

### 4.3.b 修改动态字段

将来要修改某个字段是不固定的，不一定是修改所有，是修改某个字段

假如说我们现在有这样一个新的要修改的字段，如果其他字段缺失，就会被设定为`null`

```java
// 测试类中
int status = 1;
String companyName = "遥遥领先";
String brandName = "遥遥领先";
String description = "喜欢拉踩同行营销的公司，喜欢嘲讽某米的品牌";
int ordered = 200;
int id = 13;

Brand brand = new Brand();
// brand.setBrandName(brandName);
// brand.setCompanyName(companyName);
// brand.setOrdered(ordered);
// brand.setDescription(description);
brand.setStatus(status);
brand.setId(id);
```

改进也很简单，有啥改啥，在SQL里面写上if-else判断

注意用`set` 把数据“框”起来

```xml
<update id="update">
    UPDATE tb_brand
        <set>
            <if test="brandName != null and brandName != ''">
                brand_name = #{brandName},
            </if>
            <if test="companyName != null and companyName != ''">
                company_name = #{companyName},
            </if>
            <if test="ordered != null">
                ordered = #{ordered},
            </if>
            <if test="description != null and description != ''">
                description = #{description},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
        </set>
        WHERE id = #{id}
</update>
```

## 4.4 删除

### 4.1.a 删除一个

```java
void deleteById(int id);
```

```xml
<delete id="deleteById">
		delete from tb_brand where id = #{id}
</delete>
```

### 4.1.b 批量删除

```java
void deleteByIds(int id);
```

所对应的SQL语句需要用动态SQL

但是要注意 mybatis 会把参数封装为一个Map集合

默认是array

我们在定义接口的时候可以用 `@Param` 标注一下

定义逗号为分隔符

```xml
<delete id="deleteByIds">
    delete from tb_brand where id in
    <foreach collection="array" item="id" separator="," open="(" close=")">
        #{id}
    </foreach>
</delete>
```